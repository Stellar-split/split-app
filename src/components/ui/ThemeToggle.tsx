/**
 * ThemeToggle — re-exported from src/components/ThemeToggle.tsx so it is
 * accessible from the canonical ui/ path:
 *   import ThemeToggle from "@/components/ui/ThemeToggle"
 *
 * The full implementation (icons, aria-label, cycleTheme) lives at
 * src/components/ThemeToggle.tsx and consumes ThemeContext.
 */
export { default } from "@/components/ThemeToggle";
