/**
 * useTheme — thin re-export from ThemeContext so consumers can import from
 * a predictable hooks path: import { useTheme } from "@/hooks/useTheme"
 *
 * The full ThemeProvider, ThemeMode type, and cycleTheme function live in
 * src/contexts/ThemeContext.tsx.
 */
export { useTheme } from "@/contexts/ThemeContext";
export type { } from "@/contexts/ThemeContext";
